<% layout = 'layout' %>

<h1 class="mb-4">📄 CV de <%= candidat.name || 'Candidat inconnu' %></h1>

<div class="mb-4">
  <% if (candidat.cv_filename) { %>
    <iframe
      src="/uploads/<%= encodeURIComponent(candidat.cv_filename) %>"
      width="100%"
      height="700px"
      style="border: 1px solid #ccc; border-radius: 6px;"
    ></iframe>
  <% } else { %>
    <p class="text-danger">Aucun CV disponible pour ce candidat.</p>
  <% } %>
</div>

<div class="d-flex gap-2">
  <a href="/candidates" class="btn btn-secondary">⬅ Retour à la liste</a>
  <% if (candidat.cv_filename) { %>
    <a href="/uploads/<%= encodeURIComponent(candidat.cv_filename) %>" class="btn btn-primary" target="_blank">🔗 Ouvrir dans un nouvel onglet</a>
  <% } %>
</div>
