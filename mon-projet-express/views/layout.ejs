<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title><%= typeof title !== 'undefined' ? title : 'Ménara Holding' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  
  <!-- Favicon -->
  <link rel="icon" href="/images/menara-logo.png" type="image/png">
  <link rel="shortcut icon" href="/images/menara-logo.png" type="image/png">
  
  <style>
    header {
      display: flex;
      align-items: center;
      background-color: #f8f9fa;
      padding: 10px 20px;
      border-bottom: 1px solid #dee2e6;
      margin-bottom: 20px;
    }
    header img {
      height: 50px;
      margin-right: 15px;
    }
    header h1 {
      font-size: 1.5rem;
      margin: 0;
    }
  </style>
</head>
<body>
  <header>
    <img src="/images/menara-logo.png" alt="Logo Menara Holding">
    <h1>Ménara Holding</h1>
  </header>

  <div class="container">
    <% const { user } = locals; %>
    <% if (user) { %>
      <p class="text-end">Connecté en tant que <strong><%= user.username %></strong> (<%= user.role %>) | <a href="/logout">Déconnexion</a></p>
    <% } %>
    <%- body %>
  </div>
</body>
</html>