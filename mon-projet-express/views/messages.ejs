<%- include('partials/header') %>

<div class="container my-4">
  <h1>📨 Messages reçus</h1>

  <% if (successMessage) { %>
    <div class="alert alert-success mt-3"><%= successMessage %></div>
  <% } %>

  <% if (errorMessage) { %>
    <div class="alert alert-danger mt-3"><%= errorMessage %></div>
  <% } %>

  <% if (messages.length > 0) { %>
    <ul class="list-group mt-4">
      <% messages.forEach(m => { %>
        <li class="list-group-item">
          <h5><strong><%= m.nom %></strong> — <a href="mailto:<%= m.email %>"><%= m.email %></a></h5>
          <p><strong>Message :</strong> <%= escapeHtml(m.message) %></p>
          <small class="text-muted">🕒 <%= m.date || 'Date inconnue' %></small>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p class="mt-3">Aucun message reçu pour l'instant.</p>
  <% } %>
</div>

<%- include('partials/footer') %>
