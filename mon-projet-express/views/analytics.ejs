<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau de bord - Analytiques</title>

    <!-- External Libraries -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* ===== VARIABLES CSS ===== */
        :root {
            /* Couleurs principales */
            --primary: #4a6cf7;
            --primary-dark: #3a5ae8;
            --secondary: #7d5cc6;

            /* Couleurs d'état */
            --success: #10b981;
            --warning: #f59e0b;
            --danger: #ef4444;

            /* Couleurs neutres */
            --light: #f8fafc;
            --dark: #1e293b;
            --gray: #64748b;
            --border: #e2e8f0;
            --card-bg: #ffffff;
            --hover: #f1f5f9;

            /* Espacements */
            --spacing-xs: 5px;
            --spacing-sm: 10px;
            --spacing-md: 15px;
            --spacing-lg: 20px;
            --spacing-xl: 25px;
            --spacing-2xl: 30px;

            /* Rayons de bordure */
            --radius-sm: 8px;
            --radius-md: 12px;
            --radius-lg: 16px;

            /* Ombres */
            --shadow-sm: 0 2px 10px rgba(0, 0, 0, 0.03);
            --shadow-md: 0 4px 20px rgba(0, 0, 0, 0.05);
            --shadow-lg: 0 0 20px rgba(0, 0, 0, 0.1);

            /* Transitions */
            --transition: all 0.3s ease;
        }

        /* ===== RESET & BASE ===== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: var(--light);
            color: var(--dark);
            line-height: 1.6;
        }

        /* ===== LAYOUT PRINCIPAL ===== */
        .container {
            display: flex;
            min-height: 100vh;
        }

        .main-content {
            flex: 1;
            margin-left: 250px;
            padding: var(--spacing-2xl);
        }

        /* ===== SIDEBAR / MENU LATERAL ===== */
        .sidebar {
            position: fixed;
            top: 0;
            left: 0;
            width: 250px;
            height: 100vh;
            background: linear-gradient(180deg, var(--primary), var(--primary-dark));
            color: white;
            padding: var(--spacing-lg) 0;
            box-shadow: var(--shadow-lg);
            z-index: 100;
        }

        /* Logo */
        .logo {
            display: flex;
            align-items: center;
            padding: 0 var(--spacing-xl) var(--spacing-lg);
            margin-bottom: var(--spacing-lg);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .logo h1 {
            display: flex;
            align-items: center;
            gap: var(--spacing-sm);
            font-size: 1.5rem;
            font-weight: 700;
        }

        .logo i {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 36px;
            height: 36px;
            background: white;
            color: var(--primary);
            border-radius: var(--radius-sm);
        }

        /* Menu de navigation */
        .menu {
            padding: 0 var(--spacing-md);
        }

        .menu a {
            display: flex;
            align-items: center;
            gap: var(--radius-md);
            padding: var(--radius-md) var(--spacing-lg);
            margin-bottom: var(--spacing-xs);
            color: rgba(255, 255, 255, 0.9);
            text-decoration: none;
            border-radius: var(--radius-sm);
            transition: var(--transition);
        }

        .menu a:hover,
        .menu a.active {
            background: rgba(255, 255, 255, 0.15);
            color: white;
        }

        .menu a i {
            width: 24px;
            font-size: 18px;
            text-align: center;
        }

        /* ===== HEADER ===== */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: var(--spacing-2xl);
        }

        .header h2 {
            display: flex;
            align-items: center;
            gap: var(--spacing-sm);
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--dark);
        }

        /* Informations utilisateur */
        .user-info {
            display: flex;
            align-items: center;
            gap: var(--spacing-md);
        }

        .notification {
            position: relative;
            font-size: 1.2rem;
            color: var(--gray);
            cursor: pointer;
        }

        .notification::after {
            content: '3';
            position: absolute;
            top: -5px;
            right: -5px;
            display: flex;
            align-items: center;
            justify-content: center;
            width: 18px;
            height: 18px;
            background: var(--danger);
            color: white;
            font-size: 0.7rem;
            border-radius: 50%;
        }

        .user-profile {
            display: flex;
            align-items: center;
            gap: var(--spacing-sm);
        }

        .user-profile img {
            width: 40px;
            height: 40px;
            border: 2px solid var(--primary);
            border-radius: 50%;
            object-fit: cover;
        }

        /* ===== CARTES DE STATISTIQUES ===== */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: var(--spacing-lg);
            margin-bottom: var(--spacing-2xl);
        }

        .stat-card {
            background: var(--card-bg);
            padding: var(--spacing-xl);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-md);
            transition: transform 0.3s ease;
        }

        .stat-card:hover {
            transform: translateY(-5px);
        }

        .stat-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: var(--spacing-md);
        }

        .stat-info {
            flex: 1;
        }

        .stat-title {
            font-size: 0.9rem;
            font-weight: 500;
            color: var(--gray);
            margin-bottom: 8px;
        }

        .stat-value {
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--dark);
            line-height: 1.2;
        }

        .stat-change {
            display: flex;
            align-items: center;
            gap: var(--spacing-xs);
            font-size: 0.85rem;
            margin-top: 8px;
        }

        .stat-change.positive {
            color: var(--success);
        }

        .stat-change.negative {
            color: var(--danger);
        }

        /* Icônes des statistiques */
        .stat-icon {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 50px;
            height: 50px;
            border-radius: var(--radius-md);
            font-size: 1.5rem;
        }

        .icon-users {
            background: rgba(16, 185, 129, 0.15);
            color: var(--success);
        }

        .icon-offers {
            background: rgba(122, 93, 214, 0.15);
            color: var(--secondary);
        }

        .icon-applications {
            background: rgba(239, 68, 68, 0.15);
            color: var(--danger);
        }

        .icon-matching {
            background: rgba(245, 158, 11, 0.15);
            color: var(--warning);
        }

        /* ===== CONTENU / CARTES ===== */
        .content {
            background: var(--card-bg);
            padding: var(--spacing-xl);
            margin-bottom: var(--spacing-2xl);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-md);
        }

        .content h3 {
            font-size: 1.3rem;
            margin-bottom: var(--spacing-lg);
            padding-bottom: var(--spacing-md);
            color: var(--dark);
            border-bottom: 1px solid var(--border);
        }

        /* ===== TABLEAU ===== */
        .table-container {
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            border-radius: var(--spacing-sm);
            overflow: hidden;
            box-shadow: var(--shadow-sm);
        }

        thead {
            background: var(--primary);
            color: white;
        }

        th, td {
            padding: var(--spacing-md);
            text-align: left;
        }

        th {
            font-weight: 600;
        }

        tbody tr {
            border-bottom: 1px solid var(--border);
        }

        tbody tr:last-child {
            border-bottom: none;
        }

        tbody tr:nth-child(even) {
            background: var(--light);
        }

        tbody tr:hover {
            background: var(--hover);
        }

        /* ===== GRAPHIQUE ===== */
        .chart-container {
            background: var(--card-bg);
            padding: var(--spacing-lg);
            margin-top: var(--spacing-2xl);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-md);
        }

        .chart-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: var(--spacing-lg);
        }

        .chart-header h3 {
            margin: 0;
            padding: 0;
            border: none;
        }

        .chart-actions {
            display: flex;
            gap: var(--spacing-sm);
        }

        .chart-actions button {
            padding: var(--radius-sm) var(--spacing-md);
            background: white;
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            cursor: pointer;
            transition: var(--transition);
        }

        .chart-actions button:hover,
        .chart-actions button.active {
            background: var(--primary);
            color: white;
            border-color: var(--primary);
        }

        #chartContainer {
            position: relative;
            height: 300px;
        }

        /* ===== RESPONSIVE DESIGN ===== */
        @media (max-width: 992px) {
            .sidebar {
                width: 80px;
            }

            .main-content {
                margin-left: 80px;
            }

            .logo h1 span,
            .menu a span {
                display: none;
            }

            .logo h1 {
                justify-content: center;
            }

            .menu a {
                justify-content: center;
            }
        }

        @media (max-width: 768px) {
            .main-content {
                padding: var(--spacing-lg);
            }

            .stats-grid {
                grid-template-columns: 1fr;
            }

            .header {
                flex-direction: column;
                align-items: flex-start;
                gap: var(--spacing-md);
            }

            .user-info {
                align-self: flex-end;
            }

            .chart-header {
                flex-direction: column;
                gap: var(--spacing-md);
                align-items: flex-start;
            }
        }

        @media (max-width: 576px) {
            .sidebar {
                width: 60px;
            }

            .main-content {
                margin-left: 60px;
                padding: var(--spacing-md);
            }

            .stat-card {
                padding: var(--spacing-lg);
            }

            .content {
                padding: var(--spacing-lg);
            }
        }
    </style>
</head>

<body>
<!-- ===== STRUCTURE PRINCIPALE ===== -->
<div class="container">
    <!-- ===== SIDEBAR / MENU LATERAL ===== -->
    <div class="sidebar">
        <div class="logo">
            <h1>
                <i class="fas fa-chart-line"></i>
                <span>AnalyticsPro</span>
            </h1>
        </div>

        <nav class="menu">
            <a href="/analytics" class="active">
                <i class="fas fa-chart-bar"></i>
                <span>Analytiques</span>
            </a>
            <a href="/reports">
                <i class="fas fa-file-alt"></i>
                <span>Rapports</span>
            </a>
            <a href="/settings">
                <i class="fas fa-cog"></i>
                <span>Paramètres</span>
            </a>
        </nav>
    </div>

    <!-- ===== CONTENU PRINCIPAL ===== -->
    <main class="main-content">

        <!-- Header avec titre et infos utilisateur -->
        <header class="header">
            <h2>
                <i class="fas fa-chart-bar"></i>
                Tableau de bord analytique
            </h2>

            <div class="user-info">
                <div class="notification">
                    <i class="fas fa-bell"></i>
                </div>
                <div class="user-profile">
                    <img src="https://randomuser.me/api/portraits/women/43.jpg" alt="Profil utilisateur">
                    <span>Hanouna Chaimae</span>
                </div>
            </div>
        </header>

        <!-- ===== CARTES DE STATISTIQUES ===== -->
        <section class="stats-grid">
            <div class="stat-card">
                <div class="stat-header">
                    <div class="stat-info">
                        <div class="stat-title">Utilisateurs actifs</div>
                        <div class="stat-value">15</div>
                    </div>
                    <div class="stat-icon icon-users">
                        <i class="fas fa-users"></i>
                    </div>
                </div>
                <div class="stat-change positive">
                    <i class="fas fa-arrow-up"></i>
                    12.5% depuis le mois dernier
                </div>
            </div>

            <div class="stat-card">
                <div class="stat-header">
                    <div class="stat-info">
                        <div class="stat-title">Offres publiées</div>
                        <div class="stat-value">4</div>
                    </div>
                    <div class="stat-icon icon-offers">
                        <i class="fas fa-briefcase"></i>
                    </div>
                </div>
                <div class="stat-change positive">
                    <i class="fas fa-arrow-up"></i>
                    8.3% depuis le mois dernier
                </div>
            </div>

            <div class="stat-card">
                <div class="stat-header">
                    <div class="stat-info">
                        <div class="stat-title">Candidatures reçues</div>
                        <div class="stat-value">20</div>
                    </div>
                    <div class="stat-icon icon-applications">
                        <i class="fas fa-file-signature"></i>
                    </div>
                </div>
                <div class="stat-change negative">
                    <i class="fas fa-arrow-down"></i>
                    3.2% depuis le mois dernier
                </div>
            </div>

            <div class="stat-card">
                <div class="stat-header">
                    <div class="stat-info">
                        <div class="stat-title">Taux de matching moyen</div>
                        <div class="stat-value">78%</div>
                    </div>
                    <div class="stat-icon icon-matching">
                        <i class="fas fa-percentage"></i>
                    </div>
                </div>
                <div class="stat-change positive">
                    <i class="fas fa-arrow-up"></i>
                    4.7% depuis le mois dernier
                </div>
            </div>
        </section>

        <!-- ===== TABLEAU DES DONNEES ===== -->
        <section class="content">
            <h3>Détails des métriques</h3>
            <div class="table-container">
                <table>
                    <thead>
                    <tr>
                        <th>Métrique</th>
                        <th>Valeur</th>
                        <th>Évolution</th>
                        <th>Statut</th>
                    </tr>
                    </thead>
                    <tbody>
                    <tr>
                        <td>Utilisateurs actifs</td>
                        <td>15</td>
                        <td>+12.5%</td>
                        <td><span class="positive">Excellent</span></td>
                    </tr>
                    <tr>
                        <td>Offres publiées</td>
                        <td>4</td>
                        <td>+8.3%</td>
                        <td><span class="positive">Bon</span></td>
                    </tr>
                    <tr>
                        <td>Candidatures reçues</td>
                        <td>20</td>
                        <td>-3.2%</td>
                        <td><span class="negative">Attention</span></td>
                    </tr>
                    <tr>
                        <td>Taux de matching moyen</td>
                        <td>78%</td>
                        <td>+4.7%</td>
                        <td><span class="positive">Très bon</span></td>
                    </tr>
                    <tr>
                        <td>Utilisateurs satisfaits</td>
                        <td>92%</td>
                        <td>+1.8%</td>
                        <td><span class="positive">Excellent</span></td>
                    </tr>
                    <tr>
                        <td>Temps de réponse moyen</td>
                        <td>12h</td>
                        <td>-2.4h</td>
                        <td><span class="positive">Amélioration</span></td>
                    </tr>
                    </tbody>
                </table>
            </div>
        </section>

        <!-- ===== GRAPHIQUE ===== -->
        <section class="chart-container">
            <div class="chart-header">
                <h3>Répartition des données</h3>
                <div class="chart-actions">
                    <button class="active" onclick="switchPieChart('overview')">Vue d'ensemble</button>
                    <button onclick="switchPieChart('candidates')">Candidats détaillé</button>
                </div>
            </div>
            <div id="pieChartContainer">
                <canvas id="pieChart"></canvas>
            </div>
        </section>

    </main>
</div>

<!-- ===== SCRIPTS ===== -->
<script src="/js/chart.js"></script>
<script id="pie-data" type="application/json">
  <%- JSON.stringify(pieData || {}) %>


</script>
<script src="/js/chartSelf.js"></script>
</body>
</html>