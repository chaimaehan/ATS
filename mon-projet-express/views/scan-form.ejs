<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Scan personnalisé ATS</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

  <style>
    body {
      background: #f4f6f9;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    h2 {
      text-align: center;
      color: #3f37c9;
      margin-top: 30px;
      font-weight: bold;
    }

    .form-container {
      max-width: 600px;
      margin: 40px auto;
      padding: 25px 30px;
      background: #fff;
      border-radius: 15px;
      box-shadow: 0 6px 20px rgba(0,0,0,0.1);
      transition: transform 0.2s ease-in-out;
    }

    .form-container:hover {
      transform: translateY(-5px);
    }

    label {
      font-weight: 500;
      color: #333;
    }

    .form-control {
      border-radius: 10px;
      box-shadow: none;
      transition: all 0.2s ease-in-out;
    }

    .form-control:focus {
      border-color: #3f37c9;
      box-shadow: 0 0 6px rgba(63, 55, 201, 0.3);
    }

    button.btn-primary {
      background: #3f37c9;
      border: none;
      border-radius: 10px;
      padding: 10px 20px;
      transition: background 0.3s ease;
    }

    button.btn-primary:hover {
      background: #4361ee;
    }
  </style>
</head>
<body>

  <div class="form-container">
    <h2>🔍 Scan ATS personnalisé</h2>

    <form method="POST" action="/scan-custom" class="mt-4">
      <div class="mb-3">
        <label for="keywords" class="form-label">Mots-clés à rechercher (séparés par des virgules)</label>
        <textarea class="form-control" id="keywords" name="keywords" rows="3" placeholder="Exemple : JavaScript, Python, React, SQL"></textarea>
      </div>
      <button type="submit" class="btn btn-primary"><i class="fa-solid fa-magnifying-glass"></i> Lancer le scan</button>
    </form>
  </div>

</body>
</html>
